{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["config:recommended", ":dependencyDashboard", ":semanticCommits"],
  "labels": ["renovate"],
  "prConcurrentLimit": 10,
  "prHourlyLimit": 20,
  "packageRules": [
    {
      "description": "Auto-merge @orangecheck/* family bumps (patch+minor) — bundled into one PR, 1-day stability window.",
      "matchPackageNames": ["/^@orangecheck//"],
      "matchUpdateTypes": ["patch", "minor"],
      "groupName": "@orangecheck/* (family)",
      "automerge": true,
      "automergeType": "branch",
      "minimumReleaseAge": "1 day"
    },
    {
      "description": "Auto-merge ALL OTHER patch + minor bumps once CI is green — 1-day stability window. If your test/typecheck/build catches a regression, the merge is automatically blocked.",
      "matchUpdateTypes": ["patch", "minor"],
      "automerge": true,
      "automergeType": "branch",
      "minimumReleaseAge": "1 day"
    },
    {
      "description": "Human-review major bumps — wire-shape or breaking changes.",
      "matchUpdateTypes": ["major"],
      "automerge": false,
      "labels": ["renovate", "major-bump"]
    }
  ]
}
