/**
 * @orangecheck/design
 *
 * The OrangeCheck design system: multi-theme tokens, primitives, and family
 * composites for the .ochk.io sub-sites. Family-internal — for embeddable
 * OrangeCheck components see @orangecheck/react.
 *
 * Import the CSS once in your app's globals:
 *   @import '@orangecheck/design/styles.css';
 *
 * Subpath entries are also available: `@orangecheck/design/tokens`,
 * `/primitives`, `/composites`, `/chrome`, `/components` (back-compat),
 * `/format`.
 */
export * from './tokens';
export * from './primitives';
export * from './composites';
export * from './chrome';
export * from './format';
